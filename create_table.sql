create table if not exists my_pokemon (
    id              text,
    name            text,
    caught_date     timestamp,
    level           integer,
    is_rad          boolean,
    load_time       timestamp
);


